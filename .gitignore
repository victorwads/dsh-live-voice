node_modules/
# dsh.pub installs from a pinned GitHub commit and validates these runtime
# entries without executing the source build.
lib/*
!lib/client.js
!lib/server.js
*.tgz
coverage/
artifacts/
.test-dist/
.npm-cache/
.DS_Store

# Local speech runtimes, model weights, logs, and generated audio. These files
# are machine-specific and can be many gigabytes; only the setup scripts and
# documentation belong in Git.
.local-voice/
